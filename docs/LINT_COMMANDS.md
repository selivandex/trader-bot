# Команды для проверки кода

## 🚀 Быстрая проверка (перед коммитом)

```bash
make check
```

Проверяет:
- ✅ Неиспользуемые переменные (`go vet`)
- ✅ Компиляцию кода (`go build`)
- ✅ Форматирование (`gofmt`)

## 📋 Все доступные команды

### Форматирование

```bash
make fmt                # Автоформатирование всего кода
```

### Проверки качества

```bash
make check              # Быстрая проверка (рекомендуется)
make lint-unused        # Поиск неиспользуемого кода
make lint-all           # Полная статическая проверка
```

### golangci-lint (пока не работает с Go 1.25+)

```bash
make lint               # Запуск golangci-lint (будет ошибка)
make lint-fix           # С автоисправлением
```

## 🎯 Текущий статус проекта

### ✅ Что работает сейчас

- **go vet**: Проверяет неиспользуемые переменные, подозрительные конструкции
- **go build**: Проверяет компиляцию
- **gofmt**: Проверяет и исправляет форматирование

### ⏳ Что будет работать позже

- **golangci-lint**: Когда добавят поддержку Go 1.25+
  - Более детальные проверки
  - Больше линтеров
  - Автоисправление

## 🔧 Что делать перед коммитом

```bash
make fmt && make check
```

## 📊 Результаты последней проверки

Все проверки пройдены успешно:
- ✅ `go vet` - неиспользуемых переменных не найдено
- ✅ `go build` - код компилируется
- ✅ `gofmt` - код правильно отформатирован

## 🐛 Если нашли проблему

### Неиспользуемые переменные

```bash
make lint-unused
```

Покажет файлы с проблемами. Затем:
1. Удалите неиспользуемые переменные
2. Закомментируйте с `_` если нужно: `_ = unusedVar`

### Неправильное форматирование

```bash
make fmt    # Автоматически исправит
```

### Ошибки компиляции

```bash
go build ./...   # Покажет ошибки
```

## 📚 Подробная документация

См. `docs/LINTING.md` для:
- Детального описания всех инструментов
- Настройки pre-commit хуков
- Интеграции с CI/CD
- Альтернативных инструментов

## ⚡ Быстрая справка

| Команда | Когда использовать |
|---------|-------------------|
| `make fmt` | Перед коммитом |
| `make check` | Перед коммитом |
| `make lint-unused` | Для очистки кода |
| `make lint-all` | Перед PR |
| `make test` | Всегда! |

## 🎓 Почему не golangci-lint?

**Причина**: golangci-lint пока не поддерживает Go 1.25.3

**Решение**: Используем нативные инструменты Go:
- `go vet` - ловит 90% проблем
- `go build` - проверяет компиляцию
- `gofmt` - форматирование

**Когда заработает**: Как только golangci-lint обновится до Go 1.25+

## ✅ Итог

1. **Для ежедневной работы**: `make check`
2. **Для очистки кода**: `make lint-unused`
3. **Перед PR**: `make fmt && make lint-all && make test`

Всё настроено и готово к использованию! 🎉

